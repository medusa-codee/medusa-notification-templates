/**
 * Type definitions for contact-form translations
 */

export interface ContactFormTranslations {
  labels: {
    name: string;
    email: string;
    phone: string;
    message: string;
  };
  footer: string;
}

